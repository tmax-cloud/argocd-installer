apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitlab-tmax.fullname" . }}
  labels:
    {{- include "gitlab-tmax.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitlab-tmax.selectorLabels" . | nindent 4 }}
